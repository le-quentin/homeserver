---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ volume.name }}"
  namespace: "{{ k3s_service_namespace }}"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-dynamic
  resources:
    requests:
      storage:  "{{ volume.size }}"
